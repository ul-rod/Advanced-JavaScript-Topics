/**
 * 9.44
 * Module Pattern
 * A module is a chunk of code that handles a discreet set of tasks.
 * A module may be placed in it's own JS file to make them easier to manage.
 * A module allows you to organize your code by grouping things that go
 * together naturally.
 */
